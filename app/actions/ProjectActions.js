import alt from '../alt';

class ProjectActions {
    constructor() {
        this.generateActions(
            'getDefaultMessgae'
        );
    }
}

export default alt.createActions(ProjectActions);